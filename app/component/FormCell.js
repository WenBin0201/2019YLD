import React from 'react'import PropTypes from 'prop-types'import { Text, StyleSheet, PixelRatio, View, TouchableOpacity, CheckBox } from 'react-native'export const TYPE_TEXT: number = 1export const TYPE_CHECK_BOX: number = 2export const TYPE_BUTTON: number = 3export default class FormCell extends React.Component {    static propTypes = {        customStyle: PropTypes.object,        type: PropTypes.oneOf([ TYPE_TEXT, TYPE_CHECK_BOX, TYPE_BUTTON ])    }    static defaultProps = {        type: TYPE_TEXT    }    state = {        checked: false    }    _renderText = () => (        <Text            style={[ styles.text, this.props.customStyle ]}            {...this.props}        >            {this.props.children}        </Text>    )    _renderCheckBox = () => {        return (            <View style={[ styles.text, styles.checkBoxContainer, this.props.customStyle ]}>                <CheckBox                    value={this.state.checked}                    onValueChange={(value) => this.setState({ checked: value })}                    {...this.props}                />            </View>        )    }    _renderButton = () => {        return (            <TouchableOpacity style={[ styles.text, this.props.customStyle ]}>                <Text style={[ styles.buttonText ]} {...this.props}>{this.props.children}</Text>            </TouchableOpacity>        )    }    render(): React.ReactNode {        if ( this.props.type === TYPE_TEXT )            return this._renderText()        if ( this.props.type === TYPE_CHECK_BOX )            return this._renderCheckBox()        if ( this.props.type === TYPE_BUTTON )            return this._renderButton()    }}const styles = StyleSheet.create({    text: {        fontSize: 12,        borderRightWidth: 1 / PixelRatio.get(),        borderRightColor: 'black',        height: 24,        textAlign: 'center',        textAlignVertical: 'center'    },    checkBoxContainer: {        alignItems: 'center',        justifyContent: 'center'    },    buttonContainer: {        alignItems: 'stretch'    },    buttonText: {        flex: 1,        textAlign: 'center',        textAlignVertical: 'center',        fontSize: 14,        borderRadius: 4,        backgroundColor: 'rgb(236,105,65)',        margin: 3,        color: 'white'    }})